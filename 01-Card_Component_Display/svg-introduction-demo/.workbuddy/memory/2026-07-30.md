# 2026-07-30

## 任务：Jasper.ai 三卡片版块复刻 → 4 卡片改版

- **来源**：用户截图 `Jasper.ai` 首页 "The Jasper Platform" 下的 Agents / Content Pipelines / Jasper IQ 三张卡片
- **目标改版**：扩展为 4 张卡片，分别对应 **Agent（智能体）/ Workflow（工作流）/ Robot（机器人）/ Web（网页）** 四个产品方向
- **输出**：`D:\素材\产品介绍\design.md` — 作为 AI / 前端复刻用的提示词文档
- **关键技术结论**：
  - 网格底纹 = CSS 双层 `linear-gradient` + `background-size: 28px 28px`，**完全不需要位图**
  - 中央插图 = **全部内联 SVG**（线稿 + 半透明色块）
  - 入场动效 = IntersectionObserver 触发淡入上滑，卡片错峰 80ms
  - hover = 卡片上浮 6px + SVG 微放大 + 箭头右移 6px
  - 字体 = Google Fonts `DM Serif Display`（标题）+ `Inter`（描述）
  - 配色 = 绿/粉/蓝/紫 4 个低饱和色，Hex 已写入 design.md 第 2.3 节
- **用户**尚未指定 4 个分类的最终命名/描述，使用的是占位文案，需后续确认

## 第二轮反馈（17:44）

用户指出两个问题并提供了新截图 `{EFC7AF9A-B069-4D88-97E8-35BC564D185E}.png`：

1. **Agent 图案不符合** → 用户明确要求"直接借用该网站的样式"，即仿 Jasper.ai 原站 Agents 卡：三个文档 + 笑脸 + 绿色角标 + 装饰（三角/星/加号）。**重做**。
2. **Workflow 浮动时错位** → 原稿把每个节点和连接线拆成独立 `float-y` 组，节点浮动时连线没动，导致脱节。**修复**：将整个流程图（左框 + 箭头 + 连接线 + 右框）打包成**同一个 `<g class="float-y">` 整体浮动**；只有红色三角作为独立装饰可以单独 float。
3. **Workflow 风格** → 用户要求"借鉴该网站的 content pipelines 模块"，即圆角矩形 + 箭头 + "右→下→右"连接线 + 红色实心三角。重做。

两个文件的对应段落均已同步更新：
- `design.md` §4.1（Agent）、§4.2（Workflow）
- `index.html` 的 ① Agent 卡片 SVG、② Workflow 卡片 SVG

## 用户偏好备注

- 用户使用中文交流
- 工作区在 `D:\素材\产品介绍\`，**目前为空**，所有产出都从 design.md 开始
- 用户倾向**直接产出可复用的提示词文档**而非仅口述方案

## 第三轮反馈（17:50）— Workflow 错位/不连贯二次修复

- 用户反馈 Workflow 图标"错位不协调"。
- **根因**：第二轮虽把流程图整体浮动，但两个节点框仍分别落在上下不同高度（y=30~52 与 y=60~82），且连接线"右→下→左"的终点 (76,70) 扎进了右框内部 → 视觉错位、不连贯。
- **修复**：重做成**单条水平基线 y=61** 上的两段流程——输入节点(圆角矩形) → 箭头 → 处理节点(圆角矩形) → 箭头 → 末端红色实心三角（"运行/播放"按钮）。所有节点同高、箭头在框间干净连接、绝不扎进框内。两个框+两条箭头仍在同一个 `<g class="float-y">` 整体浮动，红色三角独立 float。
- 同步更新 `design.md` §4.2 与 `index.html` ② 卡片 SVG。

## 待跟进

- [ ] 确认 4 个分类（Agent / Workflow / Robot / Web）是否就是最终命名
- [ ] 确认描述文案是否要替换为用户自己的产品话术
- [ ] 确认品牌主色（当前用的是 Jasper 同款绿粉蓝紫四色，可能需要换成用户 VI 色）
- [ ] 如需落地实现，可在 design.md 第 6 节"一句话精简提示词"基础上直接交给 AI 生成代码
