# 2026-08-01

## 清理无用文件
- 删除 `D:\素材\card` 根目录下无用文件：`CardSwapDemo.tsx`（基于错误 Tailwind 假设的过时示例）、`CardSwapPreview.html`（单文件 CDN 预览，不稳定且类名不对齐）、`vendor/`（下载残留）。仅保留 `card-swap-demo/` 作为有效预览项目。

## card-swap-demo 重建 + 居中
- 之前 src/ 文件丢失，已重建完整 Vite(React+TS) 项目：真实 `CardSwap.tsx` / `CardSwap.css`、`App.tsx`（标题 工作流/机器人/网页 + 图片占位）、`index.css`、`main.tsx`、`index.html`。
- `CardSwap.css` 改为**居中展示**：`.card-swap-container` 由 `bottom:0;right:0;translate(5%,20%)` 改为 `top:50%;left:50%;translate(-50%,-50%)`，响应式 media 同步 `translate(-50%,-50%) scale(...)`。
- `npm install --registry=https://registry.npmmirror.com`（69 包）成功；`npm run dev` 已启动于 http://localhost:5173/。

## 备注
- 图片用内联 SVG data URI 占位（离线可用）；换成真实图片时改 `App.tsx` 中 `cards[].img` 为图片路径即可。
- 依赖安装用 npmmirror 源（默认 npmjs 在本环境慢/不稳）。
