# 2026-07-31

- 解答了 CardSwap 组件的用法：卡片内容由 `<Card>` 子元素完全自定义，可放任意 JSX（包括图片）。
- 创建了 `D:\素材\card\CardSwapDemo.tsx`，示例将标题改为「工作流 / 机器人 / 网页」，并在卡片内容区使用 `<img>` 展示图片。
- 创建了 `D:\素材\card\CardSwapPreview.html`：用 CDN（React18/ReactDOM18/GSAP3/Tailwind Play/Babel standalone）打包 CardSwap 的纯 JSX 版 + 使用示例，浏览器双击即可预览，免去搭建工程。占位图用内联 SVG 离线可用。
