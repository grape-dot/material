# 2026-07-30

- 分析 https://halfof8.com/journal 的页面设计：核心效果是每张 WebGL 卡片通过 `lookAt(鼠标指针)` 计算 rotation 并 tween 平滑，指针离开 resetLookAt 回正；另有距离透明度衰减（1-2√(nx²+ny²)，下限0.1）、CSS 入场 translateZ(-500px) rotateY(45°)→0、perspective 1000px。
- 产出 `D:\素材\3d\shows.md`（设计解析+源码级原理+参数表+复现代码+演示建议）和 `D:\素材\3d\shows-demo.html`（纯 CSS 3D 零依赖复现：卡片随鼠标倾斜、点击翻转、入场纵深动画，JS 语法已校验）。
