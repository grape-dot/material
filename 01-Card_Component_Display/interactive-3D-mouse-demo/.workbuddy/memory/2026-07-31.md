# 2026-07-31

- 按用户需求升级 `D:\素材\3d\shows-demo.html`：实现 halfof8 三级交互——①网格（鼠标跟随倾斜）→ ②点击卡片放大并转横排（Slider，激活卡 scale 1.22 居中，←→/分页/底部控制条切换）→ ③再点激活卡弹出详情浮层（Info，毛玻璃面板）。ESC 逐级返回。
- 真实图片机制：顶部 `WORKS` 数组按"由中间向外扩散"顺序配置（4×3 网格填充序 [5,6,1,2,9,10,4,7,0,3,8,11]，按到中心距离自动计算）；不足 12 张的槽位自动成为"待开发"占位卡（虚线描边 + ½? 水印），点详情显示「敬请期待」；img 留空用渐变占位，填入后启用下载按钮。
- 卡片 DOM 拆为 .card（布局/模式缩放）+ .card-inner（入场纵深+JS 倾斜），避免模式切换时 transform 冲突；JS 语法已用 node --check 校验。
- 同步更新 `shows.md`：新增第七节"三级交互与真实图片接入"，修订演示路径与 Demo 描述。
