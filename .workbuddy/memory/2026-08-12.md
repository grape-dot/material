# 2026-08-12

## 整理 README.md
- 重写 `D:\material\README.md`，使其与实际文件结构一致。
- 主要变化：路径 `D:\素材\` → `D:\material\`；`02-Motion_Effects_and_Animation` 新增 `page-navigation-style`（airbagstudio 滚动切换 cuboid v1/v2/v3，自带 README）、`ripple-style`（涟漪 Hero + 研究快照）、`top-navigation-bar`（空占位）；旧 `顶部` 空壳已移除。
- `film-style` 图片实际英文文件名：tilt/tile/bending/model.png + material.jpg（旧 README 用中文名，已更正）。
- 保留原有"相似但实现不同"对比归纳，扩展为 6 组。

## 定制 .gitignore
- 基于真实目录重写 `D:\material\.gitignore`（原 UTF-16LE 已转 UTF-8），逐条注释对应实际文件夹。
- 真正该忽略的集中在 `02-Motion_Effects_and_Animation/page-navigation-style/cuboid-v2/`：node_modules/（约270MB）、.next/（约59MB，含 .next/cache/.tsbuildinfo）、tsconfig.tsbuildinfo、.workbuddy/（全仓12处）。
- 实测：node_modules/.next/tsconfig.tsbuildinfo 当前均未被 git 跟踪（未误提交）；唯有 9 个 .workbuddy memory 文件（01 各 demo + 今日日志）仍被跟踪，需 `git rm --cached -r .workbuddy` 移除索引。
- 研究快照 nuxt_main.js / chunks/ / airbag_en.html（约1.3MB）作为可选忽略项，注释保留。
